# 기여 가이드

`rate-limiter`에 기여해주셔서 감사합니다.

## 시작 전

- 변경 제안 전에 기존 이슈, 예시, 문서를 먼저 확인합니다.
- 공개 모델과 SPI 계약은 하위 호환성을 우선합니다.
- 보안 이슈나 민감한 내용은 [SECURITY.md](./SECURITY.md) 절차를 따릅니다.

## 변경 원칙

- 변경은 작고 검토하기 쉬운 단위로 유지합니다.
- 동작이 바뀌면 테스트를 함께 갱신합니다.
- 공개 동작이나 공개 API가 바뀌면 문서, 예시, 변경 이력도 함께 수정합니다.
- 프레임워크 전용 구현이나 저장소 구현은 이 저장소에 두지 않습니다.

## 로컬 검증

변경 제안을 열기 전에 전체 테스트를 로컬에서 실행합니다.

```bash
./gradlew test
```

배포 동작을 변경한 경우 `.github/workflows/publish.yml`과 `build.gradle`의 릴리스 메타데이터도 같이 확인합니다.

## 변경 제안에 함께 적을 내용

- 사용자 관점에서 어떤 영향이 있는지 분명하게 적습니다.
- 호환성 위험이나 마이그레이션 포인트가 있으면 함께 적습니다.
- 후속 작업으로 남긴 내용이 있으면 명시합니다.
